# Algorithms in JavaScript

Small, focused exercises on JavaScript algorithms built as a React application.

Clone, install and run npm run dev. Check out the package.json for more npm scripts.

Example algorithms are in src/pages along with the Home page.